const config = {
    API_BASE_URL: 'https://twod3d-lottery-api.onrender.com',
    ENDPOINTS: {
        AUTH: {
            LOGIN: '/api/auth/login',
            REGISTER: '/api/auth/register'
        }
    }
};

export default config; 